formatter.name: pretty

suites:
  oboom:
    namespace: Vantoozz\Oboom
    src_path: src
    spec_path: ci/phpspec

extensions:
  - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - clover
  output:
    clover: build/logs/clover.xml